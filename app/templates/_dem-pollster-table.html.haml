%h4 Democrats
%table.t-pollster.democratic
  %th
  %th
    candidate
  %th
    pollster
  - dem_candidates.each do |cd|
    - if cd.poll_percent
      %tr.dem-candidate
        %td.cd-img Img
        %td.cd-name= cd.name
        %td.pollster= "#{cd.poll_percent}%"
